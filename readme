in otp page, write logic for old patients who enter the app. they should be redirected to home page.(If all needed fields are filled out then he is a old patient, if any of the field is missing then he is a new patient)
also in otp page, for new patient, check if he is a chronic guy from the first form and redirect to the home page; frontend
remove the chronic form logic where it checks first form
also remove the unwanted logic for patient profile; backend
add refresh tokens
call log backend
appointment backend


in frontend have logic like this:
if(user didn't fill for any part before home page){
    redirect him to that page;
    also every page should have connectivity like a pointer(that follows normal/correct flow);
}